import { ListenHtmlDomDirective } from './listen-html-dom.directive';

describe('ListenHtmlDomDirective', () => {
  it('should create an instance', () => {
    const directive = new ListenHtmlDomDirective();
    expect(directive).toBeTruthy();
  });
});
